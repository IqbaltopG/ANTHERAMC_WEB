import { PageHero } from "@/components/PageHero";
import { storeItems } from "@/data/demo";
export const metadata={title:"Store"};
export default function StorePage(){return <><PageHero eyebrow="ACCOUNT ENTITLEMENTS" title="NETWORK STORE" text="Rank dan cosmetic entitlement dengan purchase history-ready structure. Payment sengaja belum aktif sebelum merchant key, signature verification, dan reward queue dipasang."/><section className="section"><div className="container"><div className="store-safety"><span>STORE MODE</span><b>DEMO / NO PAYMENT CAPTURE</b><p>Checkout production seharusnya: gateway → verified webhook → idempotent transaction → reward queue → Minecraft bridge → entitlement ledger.</p></div><div className="store-grid store-grid-v2">{storeItems.map((item,i)=><article className={`store-card ${i===1?'featured':''}`} key={item.name}><span className="store-badge">{item.badge}</span><div className="gem">✦</div><small>NETWORK RANK</small><h2>{item.name}</h2><strong>{item.price}</strong><em>ONE-TIME ENTITLEMENT</em><ul>{item.perks.map(p=><li key={p}>✓ {p}</li>)}</ul><button className="btn btn-primary" disabled>CHECKOUT LOCKED</button></article>)}</div><div className="entitlement-band"><div><span>OWNERSHIP MODEL</span><h2>ONE PURCHASE. ONE LEDGER.</h2><p>Setiap item sebaiknya menjadi entitlement di database, bukan sekadar command sekali jalan. Itu membuat restore, refund, audit, dan multi-server sync jauh lebih waras.</p></div><div className="ledger-code"><code>payment.verified</code><span>→</span><code>entitlement.granted</code><span>→</span><code>reward.synced</code></div></div></div></section></>}
