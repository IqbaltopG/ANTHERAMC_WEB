import { PageHero } from "@/components/PageHero";
import { ProgressBar } from "@/components/ProgressBar";
import { battlePass, player } from "@/data/demo";
export const metadata={title:"Battle Pass"};
export default function BattlePassPage(){return <><PageHero eyebrow="SEASON 00 // FRACTURE PROTOCOL" title="BATTLE PASS" text="Season progression dengan free track, premium cosmetic track, missions, dan reward claim state."/><section className="section"><div className="container"><div className="bp-header"><div><span>CURRENT LEVEL</span><b>{player.battlePassLevel}</b></div><div className="bp-xp"><span>{player.battlePassXp.toLocaleString()} / {player.battlePassNext.toLocaleString()} XP</span><ProgressBar value={player.battlePassXp} max={player.battlePassNext}/><small>Complete daily, weekly, dungeon, dan world-event missions untuk season XP.</small></div><button className="btn btn-primary" disabled>PREMIUM ACTIVE</button></div><div className="bp-track"><div className="bp-labels"><span>LEVEL</span><span>FREE TRACK</span><span>PREMIUM TRACK</span><span>STATE</span></div>{battlePass.map(r=><div className={`bp-row ${r.current?'current':''}`} key={r.level}><b>{r.level}</b><span>{r.free}</span><strong>{r.premium}</strong><small>{r.claimed?'CLAIMED':r.current?'READY':'LOCKED'}</small></div>)}</div><div className="mission-grid">{[['DAILY','Dungeon Hunter','Complete 2 dungeon runs','1,200 XP'],['WEEKLY','Rift Closure','Close 12 world rifts','6,000 XP'],['WEEKLY','Guild Vanguard','Earn 20,000 guild contribution','4,500 XP'],['SEASON','Relic Mastery','Upgrade a relic to +10','12,000 XP']].map(m=><article key={m[1]}><span>{m[0]}</span><h3>{m[1]}</h3><p>{m[2]}</p><b>{m[3]}</b></article>)}</div></div></section></>}
