import { PageHero } from "@/components/PageHero";
import { pets } from "@/data/demo";

export const metadata = { title: "Collections" };
export default function CollectionsPage(){const owned=pets.filter(p=>p.owned).length;return <><PageHero eyebrow="ACCOUNT COLLECTION" title="COMPANION ARCHIVE" text="Track pet ownership, rarity, level, bonus, dan completion. Data demo ini siap diganti hasil sinkronisasi plugin server."/><section className="section"><div className="container"><div className="collection-summary"><div><span>DISCOVERED</span><b>{owned}/{pets.length}</b></div><div><span>COMPLETION</span><b>{Math.round(owned/pets.length*100)}%</b></div><div><span>MYTHIC OWNED</span><b>{pets.filter(p=>p.owned&&p.rarity==='MYTHIC').length}</b></div><div><span>TOTAL LEVELS</span><b>{pets.reduce((n,p)=>n+(p.owned?p.level:0),0)}</b></div></div><div className="collection-grid">{pets.map(p=><article className={`collection-card rarity-${p.rarity.toLowerCase()} ${!p.owned?'unowned':''}`} key={p.name}><span className="rarity">{p.rarity}</span><i>{p.icon}</i><h2>{p.owned?p.name:'UNDISCOVERED'}</h2><p>{p.owned?p.bonus:'Discover this companion in-game to reveal its source and ability.'}</p><div><span>{p.owned?`LEVEL ${p.level}`:'LOCKED'}</span><b>{p.owned?'OWNED':'???'}</b></div></article>)}</div></div></section></>}
